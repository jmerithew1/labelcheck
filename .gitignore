# deps / build
node_modules/
.next/
out/

# env — never commit keys
.env
.env.local
.env*.local

# misc
*.tsbuildinfo
next-env.d.ts
samples/tools/node_modules/
*.traineddata

# spike/QA outputs — regenerate with samples/tools/*.mjs; the evidence
# summaries that documents cite live in docs/*.json
samples/tools/multisignal-out/
samples/tools/multisignal-r2-out/
samples/tools/multisignal-r3-out/
samples/tools/croplens-out/
samples/tools/matrix-out/
samples/tools/locate-preview/
samples/tools/*-out/

# tesseract language data (downloaded on first OCR run)
eng.traineddata

# hard-degradation renders (regenerate: samples/tools/degrade-hard.mjs)
samples/degraded-hard/
samples/robustness/

# Real TTB COLA label images: public government records, but not ours to
# redistribute, and regenerable via samples/tools/harvest-ttb.mjs. The scored
# summary (docs/real-labels.json) and the COLA ID list ARE committed — that is
# the evidence; the pixels are not.
samples/real/
